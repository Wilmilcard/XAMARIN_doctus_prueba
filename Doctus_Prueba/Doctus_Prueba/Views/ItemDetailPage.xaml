<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Doctus_Prueba.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="35">
        <Label Text="{Binding Item.FechaCreacion}" FontSize="Large" />
        <Label Text="{Binding Item.Titulo}" FontSize="Medium" />
        <Label Text="{Binding Item.Descripcion}" FontSize="Small" />
        <Button Text="Editar" Clicked="SelectedTip" HorizontalOptions="Center" Margin="10" BackgroundColor="{StaticResource NavigationPrimary}" TextColor="White"/>
    </StackLayout>
    
</ContentPage>